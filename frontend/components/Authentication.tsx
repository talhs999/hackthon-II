'use client';

// This component is no longer needed since we're handling auth directly in the page
export default function AuthButtons() {
  return null; // We'll handle authentication directly in the page component now
}